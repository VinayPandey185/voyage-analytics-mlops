apiVersion: v1
kind: Service
metadata:
  name: travel-mlops-flask
spec:
  selector:
    app: travel-mlops-flask

  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001

  type: NodePort
